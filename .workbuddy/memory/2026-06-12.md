# 2026-06-12 工作日志

## 搭建二条律师网站（Hexo + Fluid + GitHub Pages）

### 完成
- 初始化 Hexo 8.1.2 项目 + Fluid 主题
- 配置站点结构：首页混排 /writing /projects /about /archive
- 写入 4 篇示例文章（reload/caveman/law/tools）
- 启用 RSS (atom.xml) + 本地搜索 (search.xml)
- 自定义 CSS（中文排版、引用块、暗色模式适配）
- 创建 GitHub Actions 工作流自动部署
- 推送到 azrzsthl/azrzsthl.github.io，Pages 构建成功
- 线上验证通过：https://azrzsthl.github.io

### 踩坑
- npm cache 权限问题导致 npx hexo init 失败，手动创建目录结构
- Hexo CLI 在 sandbox 环境中需要 `package.json` 里有 `hexo` 字段才能正确加载配置
- Node.js `http` 服务器收到代理风格的完整 URL 请求（`http://127.0.0.1:4000/`），需特殊处理
- themes/fluid 包含 .git 导致嵌入仓库警告，需删除 .git 后重新添加

### 待办
- 绑定 ertiao.com 自定义域名
- 替换占位内容（关于页、项目链接）
